<a href="#" id="remove-completed" {{bind-attr class="anyCompleted::hide"}} {{action "removeCompletedTasks"}}>
  <i class="icon check"></i>
  Remove completed
</a>

{{#each category in controller itemController="category"}}
  {{#view "category" editing=category.editing style=category.colorData.colorStyle}}
    <div class="category-header">
      {{color-picker selectedColor=category.color updatedColorData="updateColor"}}
      <h4 class="ui header">{{click-to-edit-text value=category.name editing=category.editing}}</h4>
      <a href="#" class="add" {{action "addTask"}}>+</a>
      <div class="right icons">
        <a href="#" class="delete" {{action "delete"}}>&times;</a>
      </div>
    </div>
    <ul class="category">
      {{#each task in category.sortedTasks itemController="task"}}
        {{view "task" data-id=task.id completed=task.completed overdue=task.overdue editing=task.editing}}
      {{/each}}
    </ul>
  {{/view}}
{{/each}}

<div id="add-category" class="ui button" aria-role="button" {{action "addCategory"}}>Add Category</div>
